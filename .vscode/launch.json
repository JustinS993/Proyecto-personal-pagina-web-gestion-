{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Abrir app (servidor en localhost:3000)",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "msedge",
            "request": "launch",
            "name": "Abrir index.html directo",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}",
            "noDebug": true
        }
    ]
}